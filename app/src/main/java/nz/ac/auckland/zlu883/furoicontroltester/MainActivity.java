package nz.ac.auckland.zlu883.furoicontroltester;import android.bluetooth.BluetoothAdapter;import android.bluetooth.BluetoothDevice;import android.os.Bundle;import android.support.design.widget.FloatingActionButton;import android.support.design.widget.Snackbar;import android.support.v7.app.AppCompatActivity;import android.support.v7.widget.Toolbar;import android.util.Log;import android.view.View;import android.view.Menu;import android.view.MenuItem;import android.widget.EditText;import java.util.Set;public class MainActivity extends AppCompatActivity {    RobotControllerHome robotController;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        Toolbar toolbar = (Toolbar) findViewById(R.id.toolbar);        setSupportActionBar(toolbar);        FloatingActionButton fab = (FloatingActionButton) findViewById(R.id.fab);        fab.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                Snackbar.make(view, "Replace with your own action", Snackbar.LENGTH_LONG)                        .setAction("Action", null).show();            }        });        robotController = RobotControllerHome.getInstance();    }    @Override    protected void onResume() {        super.onResume();        robotController.btConnect();    }    @Override    protected void onStop() {        super.onStop();        robotController.btDisconnect();    }    @Override    public boolean onCreateOptionsMenu(Menu menu) {        // Inflate the menu; this adds items to the action bar if it is present.        getMenuInflater().inflate(R.menu.menu_main, menu);        return true;    }    @Override    public boolean onOptionsItemSelected(MenuItem item) {        // Handle action bar item clicks here. The action bar will        // automatically handle clicks on the Home/Up button, so long        // as you specify a parent activity in AndroidManifest.xml.        int id = item.getItemId();        //noinspection SimplifiableIfStatement        if (id == R.id.action_settings) {            return true;        }        return super.onOptionsItemSelected(item);    }    public void sendCommand(View view) {        EditText commandText = (EditText) findViewById(R.id.edit_message);        String[] command = commandText.getText().toString().split(" ");        byte[] byteCommand = new byte[command.length];        try {            for (int i = 0; i < command.length; i++) {                byteCommand[i] = new Integer(Integer.parseInt(command[i], 16)).byteValue();                Log.i("Cmd", String.format("%02X", byteCommand[i]));            }        } catch (Exception e) {            Log.e("Bad command", e.toString());        }        robotController.sendCmdMessage(byteCommand);    }}